import * as React from "react";
import { toastsReducer } from "react-toastify-redux";

const error = message => "error";
const success = message => "success";
const ToastContainer = () => {
  return <div />;
};

export { error, success, toastsReducer, ToastContainer };
